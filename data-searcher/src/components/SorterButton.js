import React from 'react';
import '../App.css';

// this component includes a set of buttons
const SorterButton = children => <div className='buttonGroup'>{children}</div>;

export default SorterButton;
